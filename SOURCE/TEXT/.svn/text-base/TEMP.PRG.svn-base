function Main()

    local nFile := FOPen( "zzz.txt" )

    ? cFReadLine( nFile )
    ? cFReadLine( nFile )
    ? cFReadLine( nFile )
    ? cFReadLine( nFile )
    ? cFReadLine( nFile )
    ? cFReadLine( nFile )
    FClose( nFile )

return
